[project]
name = "mcp-server"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "fastmcp>=3.1.0",
    "django-server",
]

[tool.uv]
package = false

[tool.uv.sources]
django-server = { workspace = true }
